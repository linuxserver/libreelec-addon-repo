#!/bin/sh

. /etc/profile

# stop and remove container
docker stop docker.linuxserver.tautulli

# remove image from cron updater list
sed -i '/tautulli/d' /storage/.kodi/userdata/addon_data/docker.linuxserver.updater/update.sh
