#!/bin/sh

. /etc/profile

# stop and remove container
docker stop docker.linuxserver.mariadb

# remove image from cron updater list
sed -i '/mariadb/d' /storage/.kodi/userdata/addon_data/docker.linuxserver.updater/update.sh
